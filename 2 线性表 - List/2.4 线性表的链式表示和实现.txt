线性表的链式表示又称链式存储结构、非顺序映像或者链式映像
链式存储的存储表示：结点在存储器中的位置是任意的，即逻辑上相邻的数据元素在物理上不一定相邻
链式存储的线性表就是链表

链表的特点：
1.链式存储的每个结点分为数据域和指针域，数据域存放数据，指针域存放下一个结点的指针(该指针也称 链指针 )
只要知道第一个结点的指针(该指针也称 头指针 )，就能找到所有结点
因此链表是由头指针唯一确定，因此链表可以用头指针的名字来命名

具体实现文件
本文件夹下的：
单链表类型定义(C语言)
双向链表类型定义(C语言)