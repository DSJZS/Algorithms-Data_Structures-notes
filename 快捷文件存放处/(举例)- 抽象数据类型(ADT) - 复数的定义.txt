复数的定义

ADT Complex{
	D = { r1, r2 | r1, r2都是实数}
	S = {<r1, r2> | r1是实部，r2是虚部}
	assign (&C, v1,v2)
		初始条件:空的复数C已存在
		操作结果:构造复数C，r1, r2分别被赋以参数v1, v2的值。
	destroy (&C)
		初始条件:复数C已存在。
		操作结果:复数C被销毁。
	//......(见下一页)
}ADT Complex
